<!-- fixture HERMANA (plan §6): regla propia SIN <if_sid> que casa el MISMO evento
     que la base 5710.

     Es la misma regla hermana que se desplegó TEMPORALMENTE en el manager para el
     paso 0 (`preflight_demo_hermana.md`): sin <if_sid>, cuelga del grupo sshd y
     casa "Invalid user". C1 NO la ve (no hay <if_sid>); C2 SÍ: contra la captura
     `preflight_logtest_candidato.txt`, el evento que ganaba 5710 pasa a ganarlo
     100000 -> ENMASCARA (hermana). PASA solo si hay fila tipo=hermana. -->
<group name="tfg,preflight_fixture,">
  <rule id="100000" level="5">
    <if_group>sshd</if_group>
    <match>Invalid user</match>
    <description>FIXTURE HERMANA (RS3): Invalid user (mismo evento que 5710)</description>
  </rule>
</group>

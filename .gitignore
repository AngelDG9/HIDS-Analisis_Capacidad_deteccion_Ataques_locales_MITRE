# Recursos del profesor (read-only, material pesado/ajeno)
_recursos/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Logs y evidencias pesadas
*.log
*.mp4
*.pcap
*.pcapng
# Excepción R-13 (fase-03-piloto): la evidencia de las ventanas de ataque
# (times.log, sink.log) SÍ se versiona.
!Dataset/Ataques/Resultados/**/Logs/**

# STIX MITRE ATT&CK (fichero grande ~53 MB; se regenera con
# _artefactos/scripts/extraer_tecnicas_host.py). Se versionan la URL y el
# sidecar .sha256, NO el .json.
_artefactos/mitre/*.json
!_artefactos/mitre/*.sha256

# Atomic Red Team (Fase 3, bloque fase-03-atomic, plan §2): el clon es pesado
# (~0,6 GB, miles de ficheros) y NO se versiona; se versiona el sidecar
# Soporte/Ataques/atomic-red-team.version (url + commit + licencia).
Soporte/Ataques/atomic-red-team/

# Entornos y editores
.env
.env.*
.vscode/
.idea/

# Secretos del laboratorio Wazuh (Fase 2, _fases/fase-02/plan.md §3.10)
# Credenciales del dashboard/API e instalador: NUNCA a git.
Soporte/Wazuh/Configuracion/*password*
wazuh-install-files.tar
wazuh-install-files/
# Certificados y claves privadas
*.pem
*.key
id_rsa*
id_ed25519*
